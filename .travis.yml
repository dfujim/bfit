language: python

before_install:
  - sudo apt-get update
  - sudo apt-get install python3-tk
  
python: 
  - 3.5
  - 3.6
  - 3.7
  - 3.8

install:
  - pip install Cython
  - pip install numpy
  - python setup.py install
